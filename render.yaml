services:
  - type: web
    name: dr-detection
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 180
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.5
